# Progress 2026-05-02

## Ringkasan
Hari ini fokus pada perapihan tampilan sistem utama, bukan preview, agar lebih cepat dipakai untuk operasional harian.

## Selesai Hari Ini
- Memperkuat hierarchy halaman utama:
  - dashboard diberi KPI yang lebih tegas
  - daftar booking diberi ringkasan cepat
  - daftar pembayaran diberi KPI ringkas
- Memadatkan tabel utama agar lebih banyak data terlihat tanpa terasa sesak:
  - dashboard check-in mendatang
  - daftar booking
  - detail booking
  - daftar pembayaran
- Memperjelas posisi halaman detail booking sebagai pusat operasional dan finance:
  - KPI grand total, total pembayaran, sisa tagihan, dan status pembayaran ditonjolkan di atas
  - ringkasan keuangan dibuat lebih ringkas
  - komponen biaya dan riwayat pembayaran dibuat lebih mudah discan
- Memadatkan form booking utama:
  - blok identitas properti dibuat lebih jelas
  - input utama memakai density yang lebih rapat
  - preview booking dibuat sticky di desktop agar admin tidak perlu scroll bolak-balik
- Menyederhanakan alur akhir form booking:
  - bagian voucher, diskon manual, dan markup sekarang bisa dibuka-tutup
  - preview harga ringkas dikembalikan ke halaman form sebelum modal konfirmasi
  - catatan booking dihapus agar layout lebih pendek dan fokus
  - modal konfirmasi tetap dipakai sebagai pengecekan akhir sebelum simpan
- Menambahkan aturan tanggal pelunasan otomatis di booking:
  - jika DP masuk H-7 atau lebih awal, target pelunasan otomatis H-3
  - jika DP masuk mendekati hari H, target pelunasan otomatis saat check-in
  - tanggal pelunasan sekarang tampil di form booking, detail booking, dan dokumen invoice
- Menyusun dokumen rencana optimasi performa di `docs/planning/performance-optimization-plan.md`:
  - urutan audit query
  - prioritas index database
  - optimasi kalender
  - cache, queue, media, Redis, dan tooling developer
- Menyusun workflow branch deployment di `docs/planning/deployment-branch-workflow.md`:
  - `staging` untuk kerja dan uji deploy
  - `main` untuk branch hosting/production
- Menambahkan runbook deploy Laravel di `docs/planning/laravel-deployment-runbook.md`
- Menambahkan ignore untuk folder eksperimen lokal:
  - `stitch_windows_classic_dashboard/`
  - `trash/`
- Menambahkan utility UI baru di `resources/css/app.css` untuk menjaga konsistensi:
  - `ops-kpi-*`
  - `ops-panel-soft`
  - `ops-compact-table`
  - `ops-input`
  - `ops-textarea`

## Verifikasi
- `php artisan view:cache`
- `php artisan test tests/Feature/BookingFlowTest.php`
- `npm run build`
- `php artisan migrate`

## Next Step yang Disarankan
- Lanjut rapikan kalender booking utama agar visual status tanggal lebih cepat dibaca
- Rapikan halaman invoice dan audit log agar konsisten dengan density baru
- Audit ulang spacing dashboard setelah dipakai 1-2 hari oleh admin
