# Progress 2026-05-06

## Ringkasan
Hari ini fokus ke penyempurnaan workflow gallery villa/resort, terutama agar gambar yang gagal diproses tidak harus di-upload ulang dari awal.

## Selesai Hari Ini
- Menambahkan aksi `retry proses gambar` untuk gallery villa:
  - route baru `villas.images.retry`
  - controller retry di `VillaImageController`
  - job `ProcessVillaImage` bisa dijalankan ulang untuk image yang statusnya `failed`

- Menyempurnakan `VillaImageManager`:
  - file hasil proses lama (`main.webp`, `thumb.webp`) sekarang dibersihkan dulu sebelum retry
  - file asli tetap dipertahankan, jadi retry tidak merusak struktur storage yang sudah rapi

- Menyempurnakan UI gallery manager di edit villa:
  - tombol `Retry Proses` muncul hanya untuk gambar yang `failed`
  - feedback toast untuk status gagal diubah agar mengarahkan user ke tombol retry, bukan selalu upload ulang

- Menambahkan test baru untuk memastikan:
  - image `failed` bisa dikirim ulang ke queue
  - file hasil proses lama dibersihkan
  - status image kembali ke `pending`

- Menyiapkan workflow database dump terpusat untuk folder `db_pm_data`:
  - script import database: `scripts/db-import.ps1`
  - script export database: `scripts/db-export.ps1`
  - helper parser `.env`: `scripts/db-common.ps1`
  - dokumentasi implementasi: `docs/planning/database-dump-workflow.md`
  - keputusan penting: `db_pm_data` dipakai sebagai rumah dump `.sql`, bukan lokasi database MySQL live

- Menyusun rencana migrasi legacy khusus untuk:
  - tabel `vilas`
  - tabel `reservasi`
  - dokumentasi mapping disimpan di `docs/planning/legacy-vilas-reservasi-migration-plan.md`
  - keputusan penting: migrasi dilakukan bertahap lewat staging table, bukan direct import ke tabel utama

- Mengimplementasikan staging import legacy:
  - migration tabel `legacy_vilas`
  - migration tabel `legacy_reservasi`
  - model `LegacyVilla` dan `LegacyReservasi`
  - service `LegacyDumpImporter`
  - command `php artisan legacy:import-dump`
  - hasil import raw dari dump:
    - `legacy_vilas = 64`
    - `legacy_reservasi = 4268`

- Mengimplementasikan mapping legacy `vilas` ke tabel utama:
  - kolom `legacy_vila_id` dan `legacy_source` di `villas`
  - kolom `legacy_unit_code` dan `legacy_source` di `villa_units`
  - service `LegacyVillaMapper`
  - command `php artisan legacy:map-vilas`
  - hasil mapping:
    - `64 villa`
    - `70 unit`
    - `2 resort`
    - fasilitas villa ikut tersinkron ke `villa_facilities`

- Menambahkan command analisa untuk `legacy_reservasi`:
  - `php artisan legacy:analyze-reservasi`
  - hasil analisa awal:
    - total row `4268`
    - candidate booking valid `2851`
    - row mirip block tanggal `273`

## Verifikasi
- `php artisan test tests/Feature/VillaImageTest.php`
- `php artisan route:list --name=villas.images`
- `php artisan view:cache`
- `npm run build`
- parse syntax PowerShell script:
  - `scripts/db-common.ps1`
  - `scripts/db-import.ps1`
  - `scripts/db-export.ps1`
- review struktur dump legacy untuk tabel:
  - `vilas`
  - `reservasi`
- `php artisan migrate`
- `php artisan legacy:import-dump`
- `php artisan legacy:map-vilas --dry-run`
- `php artisan legacy:map-vilas`
- `php artisan legacy:analyze-reservasi`

## Catatan
- Folder `db_pm_data/` tetap dibiarkan di luar alur perubahan aplikasi karena bukan bagian fitur yang sedang dikerjakan
- Progress `2026-05-05` masih berupa sinkronisasi hasil kerja dari device lain dan belum di-commit di device ini
- Saat ini app tetap memakai MySQL biasa dari `.env`; folder `db_pm_data` baru diposisikan untuk backup / restore / arsip dump

## Next Step yang Disarankan
- Test manual gallery end-to-end:
  - upload
  - pending
  - ready
  - failed
  - retry
  - set cover
  - reorder
  - delete
- Tambahkan preview cover thumbnail di daftar villa agar gallery lebih cepat dicek dari list
- Siapkan konfigurasi disk S3-compatible untuk transisi ke Cloudflare R2 nanti
